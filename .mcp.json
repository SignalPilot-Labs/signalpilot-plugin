{
  "mcpServers": {
    "signalpilot": {
      "type": "http",
      "url": "${user_config.signalpilot_url}/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.signalpilot_token}"
      }
    }
  }
}
